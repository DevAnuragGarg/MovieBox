package com.intact.filmireview.domain.usecases

/**
 * ViewModel interacts with UseCases. UseCase acts not
 * only as an intermediary between the ViewModel and
 * Repository but can also request data from various
 * Repositories and combine it or apply additional
 * calculations.
 */

abstract class UseCase {
}